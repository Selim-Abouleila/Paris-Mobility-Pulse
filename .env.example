# -----------------------------------------------------------------------------
# Paris Mobility Pulse - Configuration
# -----------------------------------------------------------------------------
# INSTRUCTIONS:
# 1. Copy this file to .env:  cp .env.example .env
# 2. Fill in the values below.
# -----------------------------------------------------------------------------

# [REQUIRED] Your Google Cloud Project ID
# The project where resources will be deployed.
# Must match the output of `gcloud config get-value project`.
PROJECT_ID=

# [REQUIRED] Google Cloud Region
# Recommended: europe-west9 (Paris) for latency, or europe-west1 (Belgium).
REGION=europe-west9

# [OPTIONAL] Dataflow Staging/Temp Bucket Name
# If left blank, the scripts will auto-generate one: pmp-dataflow-<PROJECT_ID>
# If you provide one, it must start with gs://
# DATAFLOW_BUCKET_NAME=

# [OPTIONAL] Terraform State Bucket
# If we migrate to remote state later. For now, local state is used.
# TF_STATE_BUCKET=
